pre-commit:
  commands:
    biome:
      glob: '*.{js,jsx,ts,tsx,mjs,cjs,json,jsonc,css}'
      run: npx biome check --write --no-errors-on-unmatched --files-ignore-unknown=true --staged

commit-msg:
  commands:
    commitlint:
      run: npx commitlint --edit {1}
